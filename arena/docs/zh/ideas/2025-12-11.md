# 关于`SkillRegistry`
启动时加载一次全量技能池到只读池(使用robin_hood.h)，名字/描述放到std::shared_ptr<std::string>中共享

战斗创建时，按照双方技能 ID 构建一个子集 registry，只存指向全量池中对象的指针，在不进行IO的同时尽可能减小战斗态的增量内存。

热更新时重建全量池，切换指针；旧池上存在活动战斗时保持引用，结束后释放。

增加命中回避暴击3属性 in Stat.h // ?  
每只宠物新添加的基础数值是否相同，若不同则如何处理？

异常每回合概率解除以及最大持续回合以及条件解锁



